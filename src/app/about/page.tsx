// The entire content of the new 'About Us' page file would go here.
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function AboutPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>About Us</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is the LandShare Connect about page.</p>
      </CardContent>
    </Card>
  );
}
