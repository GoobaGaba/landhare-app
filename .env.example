# -----------------------------------------------------------------------------
# Firebase Client SDK Configuration
#
# These variables are prefixed with NEXT_PUBLIC_ so they can be accessed by the
# client-side code in your Next.js application.
#
# How to get these values:
# 1. Go to your Firebase project on https://console.firebase.google.com/
# 2. Go to Project Settings (click the gear icon).
# 3. Under the "General" tab, scroll down to "Your apps".
# 4. If you haven't already, register a new Web App.
# 5. Find your web app and click the "SDK setup and configuration" section.
# 6. Select "Config" and you will see a `firebaseConfig` object.
# 7. Copy the corresponding values into this file.
#
# IMPORTANT: After creating or updating your .env.local file with these keys,
# you MUST restart your Next.js development server.
# -----------------------------------------------------------------------------

# Firebase: The API key for your Firebase project.
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSy..._YOUR_API_KEY_HERE

# Firebase: The authentication domain for your project (e.g., your-project-id.firebaseapp.com).
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com

# Firebase: Your Firebase project's unique identifier.
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id

# Firebase: The storage bucket for your project (e.g., your-project-id.appspot.com).
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# Firebase: The messaging sender ID for Firebase Cloud Messaging.
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id

# Firebase: The app ID for your specific web application.
NEXT_PUBLIC_FIREBASE_APP_ID=1:your-app-id:web:your-web-app-id


# -----------------------------------------------------------------------------
# Google Maps API Key
#
# This key is required for the @vis.gl/react-google-maps component to display maps.
#
# How to get this value:
# 1. Go to the Google Cloud Console: https://console.cloud.google.com/
# 2. Select your project (it should be the same one you use for Firebase).
# 3. Go to "APIs & Services" > "Credentials".
# 4. Create a new API key or use an existing one.
# 5. IMPORTANT: Restrict this API key to only be usable from your website's domain
#    and ensure it only has access to the "Maps JavaScript API".
# -----------------------------------------------------------------------------
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=AIzaSy..._YOUR_GOOGLE_MAPS_API_KEY_HERE


# -----------------------------------------------------------------------------
# Genkit / Google AI (Gemini) API Key
#
# This key is used by Genkit on the server-side to make calls to the Gemini API
# for features like AI-powered suggestions.
#
# How to get this value:
# 1. Go to Google AI Studio: https://aistudio.google.com/
# 2. Click "Get API key".
# 3. Create a new API key in a project of your choice.
# 4. Copy the key here.
#
# Note: This variable does NOT need the NEXT_PUBLIC_ prefix, as it is only
# used on the server by Genkit.
# -----------------------------------------------------------------------------
GEMINI_API_KEY=AIzaSy..._YOUR_GEMINI_API_KEY_HERE
