# This is an example file. For local development, copy this file to a new
# file named ".env.local" and fill in your actual secret keys.
# DO NOT commit your .env.local file to version control.

# Firebase Client-Side Configuration (Required for Auth, Firestore, Storage)
# These MUST be prefixed with NEXT_PUBLIC_
# Get these from your Firebase project settings > General tab > Your apps > Web app
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSy...
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=...
NEXT_PUBLIC_FIREBASE_APP_ID=1:...:web:...

# Google Maps API Key (Required for map functionality)
# Get this from your Google Cloud project > APIs & Services > Credentials
# Ensure "Maps JavaScript API" is enabled for this key.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=AIzaSy...

# Genkit/Gemini API Key (Required for all AI features)
# This key is used on the server-side, so it does NOT need the NEXT_PUBLIC_ prefix.
# Get this from Google AI Studio.
GEMINI_API_KEY=AIza...
